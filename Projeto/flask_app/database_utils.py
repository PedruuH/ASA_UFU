from models import *
import database

def login(user_data):
    #conn = database.engine.connect()
    #db = database.SessionLocal()
    #user = User(user_data['nome'], user_data['email'])
    #db.add(user)
    #db.commit()
    #db.close()

def logout():
    

def get_aeroportos(user_data):
    db = database.SessionLocal()

def get_aeroportos_por_origem(user_data):  

def get_voos_cia_aerea(user_data):

def get_voos_tarifa(user_data):

def realizar_compra(user_data):

    
